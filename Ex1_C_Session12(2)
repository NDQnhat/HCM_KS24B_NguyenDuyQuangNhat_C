#include <stdio.h>
int calculateSum(int a,int b){
  return a+b;
};
int main() {
  int a,b;
  printf("nhap 2 so can tinh tong:\n");
  scanf("%d%d", &a,&b);
  printf("tong 2 so vua nhap: %d", calculateSum(a,b));
  return 0;
}
